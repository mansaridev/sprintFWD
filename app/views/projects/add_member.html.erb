<%= form_tag do %>
  <% @users.each do |user| %>
    <div>
      <%= check_box_tag "member_ids[]", user.id %>
      <%= label_tag "user_ids_#{user.id}", user.first_name %>
    </div>
  <% end %>
  <%= submit_tag "Add Users" %>
<% end %>